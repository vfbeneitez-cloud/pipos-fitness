# Test environment variables
# These are required for NextAuth imports to work, even though we mock sessions in tests

AUTH_SECRET="test-secret"
AUTH_URL="http://localhost:3000"
DEMO_MODE="true"
NEXT_PUBLIC_DEMO_MODE="true"

# Database URL is loaded from .env (or CI secrets)
# DATABASE_URL is required but should come from .env or CI
